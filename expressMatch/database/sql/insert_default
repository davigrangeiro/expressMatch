insert into user(nick, pass, enabled) values('davi', SHA1('admin'), true);
insert into authorities(user_id, authority) values (2, 'ROLE_ADMIN');
commit;

select * from user;

alter table stroke add stroke_id int(3) not null;


alter table symbol add href varchar(20) not null;


select char_length(label) from expression;

